DATABASE_URL="postgresql://postgres:devpass@localhost:5432/mytenants"
AUTH_SECRET="replace-with-a-random-32-byte-value"

# Optional. Without it, notifications are logged (not actually emailed) via a no-op sender.
RESEND_API_KEY=""
